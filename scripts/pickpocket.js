import { SETTINGS } from './settings.js';
import { GMActions } from './gmactions.js';
export class PickPocket {
    constructor(items, target, actor) {
        this.actor = actor;
        this.target = target;
        this.item;
    }

    Init() {
        // inicializador
    }

    HideCheck() {

    }
}